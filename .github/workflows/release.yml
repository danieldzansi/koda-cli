name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev

      - name: Build
        run: cargo build --release --bin koda --target x86_64-unknown-linux-gnu

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: koda-linux
          path: target/x86_64-unknown-linux-gnu/release/railway-rs

  build-macos-x86:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-apple-darwin

      - name: Build
        run: cargo build --release --bin koda --target x86_64-apple-darwin

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: koda-macos-x86
          path: target/x86_64-apple-darwin/release/railway-rs

  build-macos-arm:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Build
        run: cargo build --release --bin koda --target aarch64-apple-darwin

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: koda-macos-arm
          path: target/aarch64-apple-darwin/release/railway-rs

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build
        run: cargo build --release --bin koda --target x86_64-pc-windows-msvc

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: koda-windows
          path: target/x86_64-pc-windows-msvc/release/railway-rs.exe

  release:
    needs: [build-linux, build-macos-x86, build-macos-arm, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4

      - name: Rename binaries
        run: |
          mv koda-linux/railway-rs koda-x86_64-linux
          mv koda-macos-x86/railway-rs koda-x86_64-macos
          mv koda-macos-arm/railway-rs koda-aarch64-macos
          mv koda-windows/railway-rs.exe koda-x86_64-windows.exe

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            koda-x86_64-linux
            koda-x86_64-macos
            koda-aarch64-macos
            koda-x86_64-windows.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
